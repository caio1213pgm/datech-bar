import { MostrarNomeCompletoDirective } from './mostrar-nome-completo.directive';

describe('MostrarNomeCompletoDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarNomeCompletoDirective();
    expect(directive).toBeTruthy();
  });
});
